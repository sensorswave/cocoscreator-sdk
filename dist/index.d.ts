import type { SensorswaveConfig, UserProperties } from './types';
declare class Sensorswave {
    inited: boolean;
    private _config;
    private _store;
    private _queue;
    private _batchSender;
    private _batchSendEnabled;
    private _send;
    private _presetProps;
    private _pluginManager;
    private _emitter;
    private _sourceToken;
    private _apiHost;
    private _requestFn;
    constructor();
    init(sourceToken: string, config: SensorswaveConfig): void;
    trackEvent(eventName: string, properties?: Record<string, any>): void;
    track(eventData: {
        event: string;
        properties?: Record<string, any>;
        user_properties?: UserProperties;
        subject_properties?: Record<string, any>;
    }): void;
    identify(loginId: string): void;
    setLoginId(loginId: string): void;
    getAnonId(): string;
    getLoginId(): string;
    getPlugins(): Map<string, unknown>;
    registerCommonProperties(props: Record<string, any>): void;
    clearCommonProperties(keys?: string[]): void;
    profileSet(props: Record<string, any>): void;
    profileSetOnce(props: Record<string, any>): void;
    profileIncrement(props: Record<string, number>): void;
    profileAppend(props: Record<string, any[]>): void;
    profileUnion(props: Record<string, any[]>): void;
    profileUnset(keys: string[]): void;
    profileDelete(): void;
    checkFeatureGate(key: string): Promise<boolean>;
    getFeatureConfig(key: string): Promise<Record<string, any>>;
    getExperiment(key: string): Promise<Record<string, any> | null>;
    flush(): void;
    setDeepLinkQuery(query: Record<string, any> | string | null | undefined): void;
    destroy(): void;
    private _profileOp;
    private _enqueuePluginEvent;
    private _enqueueEvent;
    private _ensureInited;
    private _getCCSys;
    private _defaultRequestFn;
}
declare const sensorswave: Sensorswave;
export default sensorswave;
export { Sensorswave };
export type { SensorswaveConfig } from './types';
